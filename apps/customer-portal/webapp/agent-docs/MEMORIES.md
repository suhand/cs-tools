- Stack: TypeScript, React 19, Vite 7, React Router 7, Vitest, Testing Library, ESLint
- Package manager: npm
- UI library: `@wso2/oxygen-ui` (Acrylic Orange theme)
- Test command: `npm test` (Vitest)
- Lint command: `npm run lint`
- Build command: `npm run build`

- Keep page files as thin route composition; domain UI lives under `src/features/<domain>/components`; shell and cross-cutting UI under `src/components`
- Use path aliases (`@api/*`, `@components/*`, `@hooks/*`, `@features/*`, `@case-details*`, `@deployments`, `@time-tracking`, `@update-cards/*`) instead of deep relative imports; import domain constants/utils via `@features/<domain>/constants|utils/...`
- Do not modify existing tests unless explicitly requested

- Current routing is centralized in `src/App.tsx` with nested domains under `projects/:projectId`
- Major project domains in routing: dashboard, operations, support, updates, security-center, engagements, usage-metrics, settings
- Added `@api/*` and `@features/*` aliases in both `tsconfig.app.json` and `vite.config.ts`
- Domain-oriented feature roots are under `src/features/<domain>/{pages,api,components,hooks,constants,utils}`; route components import directly from concrete page files (no `src/pages`, no barrel `index.ts`)
- Dashboard cases table: props/types/enums in `features/dashboard/types/casesTable.ts` (e.g. `CaseTableSeverityColorPath`, `CaseTableMetadataKey`); copy in `constants/casesTableConstants.ts`; `getSeverityColor` / `getStatusColor` and filter/search/navigation helpers in `utils/casesTable.ts` (switch-based); `CasesTable` composes hooks + utils; `CasesTableHeader` keeps local `navigateToCreateCase` / `formatCasesTableClearFiltersLabel` to avoid cycles — reusable prompt: `agent-docs/feature-module-organization-prompt.md`
- Dashboard charts: `types/dashboardCharts.ts` (props + `OperationsChartMode` enum + `ChartLayoutProps`); copy/layout in `constants/dashboardChartsConstants.ts`; pie/legend builders in `utils/dashboardCharts.ts` (switch on mode / loading / error); `components/charts/*` stay presentational; `DashboardPage` uses `OperationsChartMode` for ops chart variant
- Engagements feature: `types/engagements.ts`, `constants/engagementsConstants.ts`, `utils/engagements.ts`, `hooks/useEngagementsPageState.ts`; `pages/EngagementsPage.tsx` composes `EngagementsStatCards` + `EngagementsListSection` only
- Support list shared helpers: `hasListSearchOrFilters` / `countListSearchAndFilters` live in `utils/listView.ts`; re-exported from `utils/support.ts` for backward compatibility; `ListSearchPanel` imports `listView` directly; all-cases UI uses `@components/list-view/{ListSearchPanel,ListItems,ListFilters,ListSkeleton}` (no support re-export stubs); `types/supportOverview.ts` (`SupportOverviewIconVariant`, card props); `AllConversationsList` in `components/all-conversations/` with list copy in `constants/conversationConstants.ts` and `resolveConversationListRowAction` in `utils/conversationsList.ts`
- Support feature types: broad component prop aggregation in `types/supportComponents.ts`; API hook variables/responses in `types/supportApi.ts`; filter/stat UI config in `types/supportUiConfig.ts`; case-creation dropdown types in `types/caseCreationOptions.ts`; create-case page state in `types/createCasePage.ts`; `DeploymentForProducts` in `types/supportHooks.ts`; `InsertImagePayload` in `types/supportRichText.ts`; `InlineAttachment` in `types/supportInlineAttachment.ts`; pages/components use `type` imports from these modules (no `export interface` in feature components)
- Announcements UI: `features/announcements/{pages,components,types,constants,utils}` — `types/announcements.ts` (`AnnouncementSortField`, `AnnouncementFilterMetadataKey`, component props + `AnnouncementFilterOption`); copy in `constants/announcementsConstants.ts`; list/search/filter/detail helpers in `utils/announcements.ts` (case search payload, `resolveAnnouncementListFilterOptions` / `buildAnnouncementsFilterSelectOptions` with `switch`, HTML normalization); `AnnouncementFilterValues` still from `@features/support/constants/supportConstants`
- Operations UI: `components/{change-requests,service-requests}`; list views `ChangeRequestsList` / `ServiceRequestsList`; CR list/calendar/filter enums + props in `types/changeRequests.ts`; SR list props + `OperationsNavSegment` + `ServiceRequestCaseSortField` + catalog/create-SR payload types in `types/serviceRequests.ts`; `constants/operationsConstants.ts` (ops page copy, CR state/impact labels, API id map, filters, stat configs, `DAYS_OF_WEEK` — data only); `utils/changeRequestUi.ts` (CR state/impact colors, icons, `formatImpactLabel`, `resolveChangeRequestCanonicalState`); `utils/operationsPages.ts`; ServiceNow `/support` deep-link redirect page in `features/project-hub/pages/ServiceNowCaseRedirectPage.tsx`
- Operations overview cards: `OutstandingChangeRequestsList` and `OutstandingChangeRequestsSkeleton` are in `features/operations/components/change-requests/`; list props type is `OutstandingChangeRequestsListProps` in `features/operations/types/changeRequests.ts`.
- Dashboard severity/chips: `types/dashboard.ts` holds `SeverityLegendKey` / `CaseTypeChipKind` / `SeverityLegendEntry` plus stat/chip types; runtime helpers `getSeverityLegendColor`, `isS0SeverityLabel`, `getSeverityFriendlyLabel`, `getCaseTypeChipConfig`, `computeCrCard*` live in `utils/dashboard.ts`; chip strings in `constants/dashboardConstants.ts`
- Cases table filter/search helpers (`countCasesTableActiveFilters`, `resolveCasesTable*`, `filterCasesTableMetadataOptions`, `mapCasesTableFilterOptionLabel`, `navigateToProjectCaseDetail`) live in `utils/casesTable.ts` with `getSeverityColor` / `getStatusColor`
- Updates feature: `types/updates.ts` (domain models + UI enums/props for stats grid, product cards, All Updates tab, PDF report); `constants/updatesConstants.ts` (stat config, tab ids, null placeholder, All Updates / grid / pending list copy); `utils/updates.ts` (aggregate stats, pending levels, chip/tooltip `switch` helpers, card header colour); `utils/allUpdatesTab.ts` (filter validation and product-level option helpers); `utils/updateLevelsReportPdf.ts` (PDF + `getUpdateLevelsReportData` using shared report types)
- Security feature: `types/security.ts` (API models + `SecurityTabId`, `CaseReportType`, `SecurityStatKey`, report-analysis enums, vulnerability chip tokens, component props); `constants/securityConstants.ts` (stat grid config, page tabs metadata, product vulnerabilities / report analysis / vulnerability details copy); `utils/securityPage.ts` (tab and report view/sort parsing); `utils/productVulnerabilitiesTable.ts` (filter count + clear label); `utils/vulnerabilities.ts` (severity/status colour helpers using enums)
- Settings feature: `types/settings.ts` (page/registry-tab enums, add-user wizard enums, AI patch kind, component/modal props); `constants/settingsConstants.ts` (null placeholder, `SETTINGS_PAGE_TABS`, role grid `ROLE_CONFIG`, user/AI/registry/add-user strings, registry regex); `utils/settings.ts` (role badges, initials, avatar color, `getRoleChipSx`); `utils/settingsPage.ts` (settings tab + registry sub-tab resolution, AI success message); `utils/registryTokens.ts` (derived token status, formatting, expiring-soon); domain API types stay in `types/users.ts` and `types/registryTokens.ts`
- Cross-cutting API hooks: `src/api` (`ApiError`, `apiConstants`, auth client, project list/filters/cases, deployments search, `apiUtils`); domain hooks live in `src/features/<domain>/api`
- Usage & metrics feature: `types/usageMetrics.ts` (inner-tab id enum, aggregated metric id enum, delta tone enum, component + API prop types), `types/timeTracking.ts`; `constants/usageMetricsConstants.ts` (chart margin, tab prefix/labels, time-range copy, overview/environment strings); `utils/usageMetricsTab.ts` (preset/date range + inner tabs; imports tab ids from types), `usageMetricsAccent.ts`, `usageMetricsAggregated.ts`, `usageMetricsEnvironmentProducts.ts`, `usageMetricDelta.ts`, `usageMetricSignedDelta.ts`, `usageMetricTrendChart.ts`; `project-details/utils/usageMetricsTab.ts` re-exports the feature utils for legacy imports; domain usage models stay in `project-details/types/usage.ts` and `utils/usageMetrics.ts`
- Project details: `types/projectDetails.ts` (`ProjectDetailsTabId` enum, `Contact`, `Stat`, `ActivityItem`, `ProjectStatusChipColor`); `types/projectDetailsComponents.ts` (all deployment/overview/time-tracking component props); `types/projectDetailsApi.ts` (deployment product/attachment/patch mutation variables, deployment products search options, license download variables); `constants/projectDetailsConstants.ts` (tabs aligned to tab id enum, shared “not available” display string, deployment document filename regexes, `UPDATE_HISTORY_INITIAL_FORM_DATA`); `utils/projectDetailsPage.ts` (tab filtering by permission uses `switch` on `ProjectDetailsTabId`), `utils/serviceHoursFormat.ts` (shared consumed/total hours display for service-hour cards)
- Project hub: `types/projectHub.ts` (`ProjectHubContentView`, `ClampedTextVariant`, card section + `ClampedTextWithTooltip` props), `types/appLayout.ts` (`AppShellNavItem` — not in constants); `constants/projectHubConstants.ts` + `constants/{navigationConstants,appLayoutConstants}.ts`; `utils/projectHub.ts` (content view + header/search layout), `utils/projectCard.ts` (dates, SLA colour, HTML strip); `ServiceNowCaseRedirectPage` uses redirect copy from `projectHubConstants`
- Shell UI: `src/components`; `src/layouts`, `src/context`, `src/providers`; `src/hooks` (`useLogger`, `useDebouncedValue`, `logger.ts`); domain types under `src/features/<domain>/types`; domain constants under `src/features/<domain>/constants`; domain utils under `src/features/<domain>/utils`; shared dropdown/error-banner constants under `features/shared/constants`; app shell / nav under `features/project-hub/constants` (includes `navigationConstants`, `appLayoutConstants`); project-details helpers: `utils/projectDetails.ts`, `utils/projectDetailsPage.ts`, `utils/addProductModal.ts`, `utils/updateHistory.ts`, `utils/usageMetrics.ts`, `utils/usageMetricsTab.ts`, `utils/timeTrackingPage.ts`, `utils/deployments.ts`; cross-app project-type permissions: `src/utils/permission.ts` with `src/types/permission.ts` and `src/constants/permissionConstants.ts`; settings helpers: `features/settings/utils` (`phone`, `settingsStorage`); idle timeout strings in `providers/authConstants.ts`; usage chart layout in `features/usage-metrics/constants`
